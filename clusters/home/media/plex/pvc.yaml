apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 60Gi  # Adjust the size as needed
  storageClassName: longhorn  # or whatever storage class you prefer
