# Never commit real secrets — set these in Portainer's stack env editor
# or a local .env file alongside the compose file.
.env
**/.env
!**/.env.example

# Claude Code local project notes (may contain secrets/tokens). The tracked
# CLAUDE.md is the shared version.
CLAUDE.local.md
