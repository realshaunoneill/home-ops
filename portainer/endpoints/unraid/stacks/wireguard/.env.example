# bcrypt hash of the wg-easy web UI password.
# Generate with: docker run --rm ghcr.io/wg-easy/wg-easy wgpw 'yourpassword'
# Note: in a plain .env file the hash does NOT need $$ escaping.
PASSWORD_HASH=$2y$10$replace-with-your-own-bcrypt-hash
