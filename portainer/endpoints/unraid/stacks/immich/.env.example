# Immich stack env vars (set these in Portainer stack Environment).

# Postgres password used by the database and immich-server services.
# Generate with:  openssl rand -base64 32
DB_PASSWORD=change-me
