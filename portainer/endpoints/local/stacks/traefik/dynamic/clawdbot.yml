http:
  routers:
    clawdbot:
      rule: "Host(`clawdbot.home.shaunoneill.com`)"
      entryPoints:
        - websecure
      service: clawdbot
      tls:
        certResolver: myresolver

  services:
    clawdbot:
      loadBalancer:
        servers:
          - url: "https://192.168.0.30:18789"
