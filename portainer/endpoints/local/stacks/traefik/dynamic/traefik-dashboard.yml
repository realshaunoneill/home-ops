http:
  routers:
    traefik:
      rule: "Host(`traefik.home.shaunoneill.com`)"
      entryPoints:
        - websecure
      service: traefik
      tls:
        certResolver: myresolver

  services:
    traefik:
      loadBalancer:
        servers:
          - url: "http://192.168.0.20:8080"
