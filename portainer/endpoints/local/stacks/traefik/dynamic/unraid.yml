http:
  routers:
    unraid:
      rule: "Host(`unraid.home.shaunoneill.com`)"
      entryPoints:
        - websecure
      service: unraid
      tls:
        certResolver: myresolver

  services:
    unraid:
      loadBalancer:
        servers:
          - url: "http://192.168.0.10"
