http:
  routers:
    prowlarr:
      rule: "Host(`prowlarr.home.shaunoneill.com`)"
      entryPoints:
        - websecure
      service: prowlarr
      tls:
        certResolver: myresolver

  services:
    prowlarr:
      loadBalancer:
        servers:
          - url: "http://192.168.0.10:9696"
