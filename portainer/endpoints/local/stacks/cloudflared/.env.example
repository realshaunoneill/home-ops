# Cloudflare Tunnel connector token (Zero Trust -> Networks -> Tunnels -> <tunnel> -> Install connector).
# ~230 chars, starts with "eyJ...". Set on the stack's Environment variables in Portainer, NOT committed here.
TUNNEL_TOKEN=
